import express from 'express';
const router  = express.Router();

router.get('tasks',);
router.post('tasks');
router.patch('tasks/:id/status');
router.delete('tasks/:id');
router.get('taks/next');

export default router;